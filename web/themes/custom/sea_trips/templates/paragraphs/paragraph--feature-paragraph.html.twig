{#
/**
 * @file
 * Default theme implementation to display a paragraph.
 *
 * Available variables:
 * - paragraph: Full paragraph entity.
 *   Only method names starting with "get", "has", or "is" and a few common
 *   methods such as "id", "label", and "bundle" are available. For example:
 *   - paragraph.getCreatedTime() will return the paragraph creation timestamp.
 *   - paragraph.id(): The paragraph ID.
 *   - paragraph.bundle(): The type of the paragraph, for example, "image" or "text".
 *   - paragraph.getOwnerId(): The user ID of the paragraph author.
 *   See Drupal\paragraphs\Entity\Paragraph for a full list of public properties
 *   and methods for the paragraph object.
 * - content: All paragraph items. Use {{ content }} to print them all,
 *   or print a subset such as {{ content.field_example }}. Use
 *   {{ content|without('field_example') }} to temporarily suppress the printing
 *   of a given child element.
 * - attributes: HTML attributes for the containing element.
 *   The attributes.class element may contain one or more of the following
 *   classes:
 *   - paragraphs: The current template type (also known as a "theming hook").
 *   - paragraphs--type-[type]: The current paragraphs type. For example, if the paragraph is an
 *     "Image" it would result in "paragraphs--type--image". Note that the machine
 *     name will often be in a short form of the human readable label.
 *   - paragraphs--view-mode--[view_mode]: The View Mode of the paragraph; for example, a
 *     preview would result in: "paragraphs--view-mode--preview", and
 *     default: "paragraphs--view-mode--default".
 * - view_mode: View mode; for example, "preview" or "full".
 * - logged_in: Flag for authenticated user status. Will be true when the
 *   current user is a logged-in member.
 * - is_admin: Flag for admin user status. Will be true when the current user
 *   is an administrator.
 *
 * @see template_preprocess_paragraph()
 *
 * @ingroup themeable
 */
#}


{% set background_type = content.field_background['#items'].0.value %}
{% set text_color = is_primary_dark_color ? 'text-white' : 'text-gray-500' %}
{% set heading_color = is_primary_dark_color ? 'text-white' : 'text-black' %}
{% set border_color = is_primary_dark_color ? 'border-white' : 'border-gray-800' %}
{% set subheading_color = is_primary_dark_color ? 'text-sky-200' : 'text-sky-400' %}
{% set background_color = background_type == 'color' ? primary_color : '#FFFFFF' %}
{% if background_type == "transparent" %}
	{% set heading_color = 'text-black' %}
	{% set subheading_color = 'text-sky-400' %}
	{% set text_color = 'text-gray-500' %}
{% endif %}
<style>
	.bg-color {
		background-color: {{background_color}}
	}
</style>
{%
  set classes = [
    'paragraph',
    'paragraph--type--' ~ paragraph.bundle|clean_class,
    view_mode ? 'paragraph--view-mode--' ~ view_mode|clean_class,
    not paragraph.isPublished() ? 'paragraph--unpublished'
  ]
%}
{% set is_image_on_the_left = content.field_image_on_the_right.0['#markup'] == 'No' ? true : false %}
{% set basisOrientation = is_image_on_the_left ? "basis-3/4" : "basis-1/2" %}
{% set imageOrientation = is_image_on_the_left ? "md:flex-row" : "md:flex-row-reverse" %}
{% set is_image_added = content.field_image|render is not empty %}
{% block paragraph %}
	<div{{attributes.addClass(classes,'mt-4','lg:mt-8','py-8')}} style="background-color: {{ background_color}}">
		{% block content %}
			<div class="py-4 px-4 md:px-8 lg:px-12 flex flex-col mt-2 xl:mt-4 mx-auto gap-x-16 lg:w-3/4
										  {{ is_image_added ? 'md:justify-start' : 'md:justify-center' }} items-start {{imageOrientation}}">
				{% if content.field_image|render %}
					<div class="features-image__wrapper mx-auto">
						{{ content.field_image }}
					</div>
				{% endif %}
				<div class="{{ basisOrientation }} flex flex-col items-center {{ is_image_added ? 'md:items-start' : 'md:items-center' }} w-full">

					{% if content.field_small_text|render %}
						<div class="{{ subheading_color }} uppercase text-sm font-main-normal font-semibold mt-4 md:mt-0">
							{{ content.field_small_text }}
						</div>
					{% endif %}
					<div class="{{ heading_color }} font-heading text-4xl mt-2 text-center md:text-left">
						{{ content.field_heading }}
					</div>
					<div class="{{ text_color }} text-base leading-6 text-center md:text-left pt-8">
						{{ content.field_description }}
					</div>
					{% if content.field_cta_link|render %}
						<a class="border {{ border_color }} transition-all hover:bg-white hover:text-black px-12 py-4 mt-4 bg-transparent rounded-md {{ text_color }} font-main-normal w-fit" href="{{ content.field_cta_link.0['#url'].toString() }}">{{ content.field_cta_link.0['#title'] }}</a>
					{% endif %}
				</div>
			{% endblock %}
		</div>
	{% endblock paragraph %}